{% extends "base.twig" %}

{% block content %}
	<div class="artist-header">
		<h1>{{post.post_title}}</h1>
		{{ include('artist-nav.twig') }}
	</div>
	<div class="artist-body">
	{% for gallery in post.get_field('artist_galleries') %}
		{{ include('artist-gallery.twig') }}
	{% endfor %}
	</div>
{% endblock %}